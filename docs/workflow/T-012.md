# T-012: Refresh active lotteries from local IAUI workbook 2026-03-25

## Goal

- Re-run the `2026-03-28` active-lottery refresh using the local workbook `Loteriju reģistrs_25.03.2026..xls` and publish site data changes only if the recomputed result differs from the current dataset.

## Scope

- Recompute the active permit set for `2026-03-28` from the local workbook.
- Compare the recomputed result with the current `assets/active_lotteries.json`.
- Update `assets/enriched_info.json` only if the workbook introduces newly active permits that can be enriched from verified public sources.
- Record whether the local workbook changes the published result compared with `T-010`.

## Implemented

- Recomputed the `2026-03-28` active permit set from the local workbook `Loteriju reģistrs_25.03.2026..xls`.
- Confirmed that the local workbook produces `44` active permits after the media filter.
- Compared the recomputed output with the current `assets/active_lotteries.json` and found no differences.
- Confirmed that permit-level contents are identical: no added permits, no removed permits, and no changed shared rows.
- Left `assets/active_lotteries.json` and `assets/enriched_info.json` unchanged because the published dataset already matches the local workbook.
- Updated the visible source card in `index.html` from `Loteriju reģistrs 16.03.2026` to `Loteriju reģistrs 25.03.2026` so the site matches the actual local workbook used for this rerun.

## Verification

- `.venv/bin/python scripts/extract_lotteries.py --date 2026-03-28 --input "Loteriju reģistrs_25.03.2026..xls" --check`
- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`
- Byte-for-byte comparison between `assets/active_lotteries.json` and the recomputed `/tmp/t012_active.json` returned identical content.
