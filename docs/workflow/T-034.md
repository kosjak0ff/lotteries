# T-034: Refresh active lotteries for 2026-05-15 from workbook 2026-05-14

## Goal

- Refresh the published Russian-language lottery site for `reference_date = 2026-05-15`.
- Use the local VID/IAUI workbook snapshot `Loteriju_reģistrs_14.05.2026..xls` as the canonical registry source.
- Remove permits that are no longer active on `2026-05-15`, add newly active permits if present, update the visible source label, and recheck missing public details from reliable public sources where available.

## Outcome

- Recomputed `assets/active_lotteries.json` from `Loteriju_reģistrs_14.05.2026..xls` for `reference_date = 2026-05-15`.
- Changed the published active permit count from `54` to `53`.
- Removed one expired permit and found no newly active non-media permits compared with the previous `2026-05-13` site state.
- Updated `index.html` so the visible source card now references `Loteriju reģistrs 14.05.2026`.
- Preserved the verified public-facing title for permit `8955` because the workbook row still has an empty `name` field.
- Updated `assets/enriched_info.json` so every active permit has exactly one enrichment entry.

## Active Permit Changes

### Added to active list

- None.

### Removed from active list

- `8879`

### Corrected active rows

- `8955` preserved `“Vasaras mērķis - tiesības + BMW?”` as the public title while using workbook-backed organizer, product, period, and location fields.

## Enrichment Changes

### Removed stale enrichment entries

- `8879`

## Public Sources Used

- Local VID workbook: `Loteriju_reģistrs_14.05.2026..xls`
- Rechecked public search results for incomplete entries `8999` and `9002`; no current public rules/prize table was found beyond the sources already recorded in `assets/enriched_info.json`.

## Notes on Incomplete Public Data

- `8999` remains conservative: public ZZ Bonusu klubs sources still did not expose current rules/prizes for permit `8999`.
- `9002` remains conservative: no public Elenger lottery page, registration form, or prize table for permit `9002` was found during this pass.

## Scope

- `assets/active_lotteries.json`
- `assets/enriched_info.json`
- `index.html`

## Verification

- `.venv/bin/python scripts/extract_lotteries.py --date 2026-05-15 --input "Loteriju_reģistrs_14.05.2026..xls" --check`
- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`
- `python -c "... parity assertions ..."`
- `.venv/bin/python -m pytest -q`
- Workbook parity check confirmed that the site permit set matches the active permit set extracted from `Loteriju_reģistrs_14.05.2026..xls`, with the only intentional divergence being the preserved human-readable title for `8955`.
