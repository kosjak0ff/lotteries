# T-022: Track workflow docs in git again

## Goal

- Restore the existing `docs/workflow` artifacts to git tracking so they are published to GitHub again.
- Preserve the current `.gitignore` change that stops excluding the workflow directory.

## Scope

- Removed the `docs/workflow/` ignore rule from `.gitignore`.
- Re-added the existing workflow artifacts `docs/workflow/T-001.md` through `docs/workflow/T-015.md` to git tracking.
- Left the already tracked later workflow artifacts (`T-016.md` onward) untouched.

## Verification

- `git ls-files docs/workflow/T-001.md docs/workflow/T-015.md docs/workflow/T-020.md`
- `git check-ignore -v docs/workflow/T-001.md || true`
- Reviewed the implementation commit to confirm only `.gitignore` and the restored workflow files were included.
