# T-011: Enrich permits 8931 and 8953

## Goal

- Find and verify public details for permits `8931` and `8953`, then publish the confirmed enrichment fields on the site.

## Scope

- Research reliable public sources for permits `8931` and `8953`.
- Confirm requirements, prizes, registration URL, timing, and other notes only where publicly verifiable.
- Update `assets/enriched_info.json` for the two permits so the site shows the richer details.

## Verification

- `python -m json.tool assets/enriched_info.json`
- `python -m json.tool assets/active_lotteries.json`
- Confirm permits `8931` and `8953` exist in `assets/enriched_info.json`.
