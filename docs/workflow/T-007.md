# T-007: Fix workflow change detection

## Goal

- Make the IAUI sync workflow detect newly created source/data files so the first successful sync can create a bot commit.

## Scope

- Update `.github/workflows/sync-iaui.yml`.
- Keep the rest of the automation unchanged.

## Verification

- `python - <<'PY'`
- check for `git add data/source assets/active_lotteries.json`
- check for `git diff --cached --quiet`
