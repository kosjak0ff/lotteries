# T-010: Refresh active lotteries for 2026-03-28

## Goal

- Refresh the published lottery dataset to match the latest IAUI registry for the reference date `2026-03-28` while preserving existing on-site details for permits that remain active.

## Scope

- Download the latest IAUI lottery registry XLS published on the official IAUI page.
- Recompute the active permit set for `2026-03-28`.
- Update `assets/active_lotteries.json` by removing inactive permits and adding newly active permits without rewriting unchanged active entries unnecessarily.
- Update `assets/enriched_info.json` only for newly added permits, filling missing public details from verified public sources and checking registration dates.

## Implemented

- Queried the official IAUI registry page and confirmed its metadata is updated as of `26.03.2026`.
- Successfully downloaded the official IAUI workbook exposed by the registry page; the attachment filename is `Loteriju reģistrs_25.03.2026..xls` and its binary contents differ from the earlier local workbook.
- Recomputed the active permit set for `2026-03-28` from the downloaded workbook and refreshed `assets/active_lotteries.json`.
- Removed inactive permits `8795` and `8830`, preserved earlier additions `8931` and `8953`, and added the newly active permit `8960`.
- Updated the published total to `44`, while keeping previously changed workbook values for permits `8755` (`end` = `2026-04-09`) and `8840` (`end` = `2026-04-07`).
- Added a verified enrichment entry for permit `8960` to `assets/enriched_info.json` using the official `automotopro.lv/loterija/` rules page.

## Notes

- The official IAUI page still shows `Atjaunināts: 26.03.2026`, and the downloaded attachment now succeeds from this environment.
- Comparing SHA-256 hashes confirmed that the downloaded workbook is newer and differs from the earlier local file used in the first T-010 pass.

## Verification

- `.venv/bin/python scripts/extract_lotteries.py --date 2026-03-28 --input /tmp/iaui_repeat.xls --check`
- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`
- Compared the previous published dataset against the refreshed file and confirmed: added `8960`; no previously published active permit was removed by the second pass.
