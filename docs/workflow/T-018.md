# T-018: Clean requirements text and show registration deadline column

## Goal

- Make the requirements cell more compact by removing redundant line breaks and duplicated registration text, and turn the current end-date column into a dedicated registration-deadline column.

## Scope

- Rename the current `Окончание` column to `Регистрация до`.
- Render that column from `registration_until` when available, with a safe fallback when the enriched deadline is missing.
- Remove `Регистрация до:` from the requirements cell so the deadline is shown only once.
- Keep venue context in the requirements cell, but without unnecessary empty spacing.

## Verification

- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`
