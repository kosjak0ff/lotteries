# T-002: Remove lottery 8830

## Goal

- Remove permit `8830` from the published site data so it no longer appears in the lottery table.

## Scope

- Update `assets/active_lotteries.json`.
- Update `assets/enriched_info.json`.
- Keep the rest of the published dataset unchanged.

## Implemented

- Removed the `permit: "8830"` entry from `assets/active_lotteries.json`.
- Decremented published `count` from `40` to `39`.
- Removed the matching `"8830"` metadata object from `assets/enriched_info.json`.

## Verification

- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`
- `rg -n '8830' assets -S`

## Notes

- `python scripts/agentctl.py verify T-002` reports a non-zero exit code because the final `rg` command returns `1` when no matches are found. In this task, that result is expected and confirms successful removal.
