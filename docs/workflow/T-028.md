# T-028: Update source card to 2026-04-22 workbook

## Goal

- Update the visible source card in `index.html` so it reflects the currently used workbook date `22.04.2026`.

## Scope

- Change only the user-visible source label in `index.html`.
- Keep the rest of the page content unchanged.

## Verification

- `rg -n "Loteriju reģistrs 22.04.2026" index.html`
