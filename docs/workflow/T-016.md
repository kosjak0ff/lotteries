# T-016: Refresh active lotteries for 2026-04-04

## Goal

- Recompute the published active-lottery dataset for `2026-04-04` from the local workbook `Loteriju_reģistrs_25.03.2026..xls`, remove permits that expired after the previous snapshot, add newly active permits, and publish verified enrichment details for the newly added rows.

## Scope

- Re-run `scripts/extract_lotteries.py` for the reference date `2026-04-04`.
- Replace `assets/active_lotteries.json` with the recomputed output if the active permit set differs from the current published snapshot.
- Update `assets/enriched_info.json` so it keeps only the active permits for `2026-04-04` and adds verified public details for new permits `8897`, `8944`, `8948`, `8950`, `8951`, `8961`, `8965`, `8966`, and `8973`.
- Keep the visible workbook source aligned with the actual local file date in `index.html`.
- Record the observed permit delta versus the published `2026-03-28` dataset: remove `8850`, `8854`, `8858`, `8867`, `8868`, `8875`, `8877`, `8883`, `8885`, `8887`, `8890`, `8892`, `8893`, `8894`, `8899`; add `8897`, `8944`, `8948`, `8950`, `8951`, `8961`, `8965`, `8966`, `8973`.

## Verification

- `.venv/bin/python scripts/extract_lotteries.py --date 2026-04-04 --input "Loteriju_reģistrs_25.03.2026..xls" --check`
- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`

## Implemented

- Recomputed the active permit set for `2026-04-04` from `Loteriju_reģistrs_25.03.2026..xls` and published the refreshed output to `assets/active_lotteries.json`.
- Updated the published snapshot from `44` active permits on `2026-03-28` to `38` active permits on `2026-04-04`.
- Removed expired permits `8850`, `8854`, `8858`, `8867`, `8868`, `8875`, `8877`, `8883`, `8885`, `8887`, `8890`, `8892`, `8893`, `8894`, and `8899` from the active dataset and from `assets/enriched_info.json`.
- Added newly active permits `8897`, `8944`, `8948`, `8950`, `8951`, `8961`, `8965`, `8966`, and `8973` to the published dataset.
- Rebuilt `assets/enriched_info.json` so its keys match the active permit order exactly and now contain only the `38` active permits for `2026-04-04`.
- Preserved existing enrichment entries for still-active permits and added verified public details for the nine new permits from public campaign pages, PDFs, or official landing pages.
- Recorded conservative fallback notes for permits `8897` and `8973`, where the official web presence confirms only part of the public details and a dedicated current rules page was not found.

## Source Notes

- `8944`, `8950`, `8951`, and `8961` were enriched from their current `loterijas.lv` campaign pages.
- `8948` was enriched from `apollokino.lv/offers/loterija` plus the linked Apollo Kino rules page.
- `8965` was enriched from the Tele2 landing page and the official PDF `Q2-Loterijas_noteikumi_Tele2.pdf` (Atļaujas Nr. `8965`).
- `8966` was enriched from `rimiloterija.lv/aprilis/` and its linked PDF rules.
- `8897` used the IAUI registry row plus official Tet pages that confirm the exact Samsung prize model, while noting that a dedicated public lottery page was not found.
- `8973` used the IAUI registry row plus the current Rīgas Komercskola homepage promo form, while explicitly excluding older rules pages tied to prior permit numbers.

## Final Verification

- `python scripts/agentctl.py verify T-016`
- Permit-key parity check confirmed that `assets/enriched_info.json` and `assets/active_lotteries.json` contain the same `38` permits in the same order.
