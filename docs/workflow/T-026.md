# T-026: Refresh active lotteries for 2026-04-24

## Goal

- Refresh the public lottery dataset so the site reflects active permits as of `2026-04-24`.
- Download the current official VID registry workbook used for the refresh.
- Remove expired permits, add newly active permits, and enrich missing public details from reliable sources where available.

## Scope

- Recompute `assets/active_lotteries.json` for `2026-04-24` from the latest official workbook snapshot.
- Update `assets/enriched_info.json` so active permits have current public-facing details where those details can be verified.
- Keep the static Russian site aligned with the refreshed data without changing unrelated UI behavior.

## Verification

- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`
- `.venv/bin/python scripts/extract_lotteries.py --date 2026-04-24 --input '<official-workbook>' --check`
