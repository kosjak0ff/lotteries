# T-005: Prepare repo for GitHub Pages

## Goal

- Add the minimal repository file needed so GitHub Pages can serve this static site directly from `main` and the repository root.

## Scope

- Add `.nojekyll` at the repository root.
- Keep the rest of the site unchanged.

## Verification

- `test -f .nojekyll`
