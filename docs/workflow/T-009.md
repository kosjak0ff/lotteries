# T-009: Ignore local IAUI registry export

## Goal

- Keep the working tree clean by ignoring the local `Loteriju reģistrs_16.03.2026..xls` source export.

## Scope

- Add a repository-level `.gitignore` entry for the specific spreadsheet filename.
- Avoid changing any tracked source data or scripts.

## Implemented

- Added a new `.gitignore` entry for `Loteriju reģistrs_16.03.2026..xls`.
- Kept the ignore rule intentionally specific to this exact local export file.

## Verification

- `python scripts/agentctl.py verify T-009`
- `git check-ignore -v "Loteriju reģistrs_16.03.2026..xls"` now resolves to `.gitignore`.
- Confirmed `git status --short` no longer shows the spreadsheet as an untracked file.
