# T-004: Configure automatic GitHub sync

## Goal

- Configure this repository so commits are pushed to `origin/main` automatically and temporary local research files do not pollute Git status.

## Scope

- Update `.gitignore` for temporary local artifacts.
- Add a local `.git/hooks/post-commit` auto-push hook.
- Verify the remaining GitHub authentication requirement.

## Implemented

- Added `tmp_*` to `.gitignore`.
- Added `wp-sitemap-posts-lottery-*.xml` to `.gitignore`.
- Installed a local executable `.git/hooks/post-commit` hook that runs `git push origin main`.
- Configured local Git credential storage and authenticated the HTTPS `origin`.
- Verified that `main` pushes to GitHub successfully and that a new commit is auto-pushed after commit creation.

## Verification

- `git check-ignore -v tmp_bing_8871.html wp-sitemap-posts-lottery-1.xml`
- `test -x .git/hooks/post-commit`
