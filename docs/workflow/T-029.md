# T-029: Refresh active lotteries for 2026-05-02 from workbook 2026-04-30

## Goal

- Refresh the public `2026-05-02` lottery dataset from the local workbook `Loteriju_reģistrs_30.04.2026..xls`.
- Remove expired permits, add newly active permits, and correct registry-backed fields that changed since the previous refresh.
- Update the visible source card so the site shows the current workbook date `30.04.2026`.
- Fill missing public participation details from reliable public sources where available.

## Scope

- Recompute `assets/active_lotteries.json` for `reference_date = 2026-05-02` from the local workbook dated `2026-04-30`.
- Update `assets/enriched_info.json` so active permits have aligned enrichment and no stale entries remain.
- Adjust `index.html` only where needed to reflect the current workbook source date.

## Verification

- `python -m json.tool assets/active_lotteries.json`
- `python -m json.tool assets/enriched_info.json`
- `python -c "import json; active=json.load(open('assets/active_lotteries.json', encoding='utf-8')); enriched=json.load(open('assets/enriched_info.json', encoding='utf-8')); permits=[row['permit'] for row in active['items']]; assert active['count']==len(permits); assert permits==sorted(permits, key=int); assert all(p in enriched for p in permits); assert all(p in permits for p in enriched); print('parity_ok')"`
